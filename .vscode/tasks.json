{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            // "command": "make",
            "command":"/usr/bin/cmake --build ${workspaceFolder}/build --config Debug --target tms --",
            "options": {
                "cwd": "${workspaceFolder}/build"
              },
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}